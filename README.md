# peek-mcp
A leightweight mcp server written in go, that reads Agentic AI sessions directly from disk and exposes them over HTTP, so a second model can evaluate what a primary agent produced without spending tokens on summarization.
